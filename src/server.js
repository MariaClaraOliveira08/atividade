// Importar a instância do Express configurada em index
const app = require("./atividade3")
//Inicia o servidor na porta 5000, neste caso a API será acessível em http://localhost:5000/
app.listen(5000);